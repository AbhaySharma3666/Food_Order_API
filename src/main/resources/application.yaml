spring:
  application:
    name: foodiesapi

# MongoDB configuration
  mongodb:
    uri: ${MONGODB_URI}

aws:
  access:
    key: ${AWS_ACCESS_KEY_ID}
    secret:
      key: ${AWS_SECRET_ACCESS_KEY}
  region: ap-south-1
  s3:
    bucket: ${S3_BUCKET_NAME}
